import SideBar from "./_components/SideBar";

const page = () => {
   return (
      <main className="px-[3.5vw] my-[2vw]">
         <SideBar />
         <div></div>
      </main>
   );
};

export default page;
